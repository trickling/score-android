<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:id="@+id/tally_view_container"
    tools:ignore="MergeRootFrame" >

    <android.support.v7.widget.Toolbar
        android:id="@+id/my_toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="?attr/colorPrimaryDark"
        android:titleTextColor="@color/white"
        android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
        android:popupTheme="@style/ThemeOverlay.AppCompat.Dark"/>

    <!--android:elevation="4dp"-->

    <LinearLayout
        android:orientation="vertical"
        android:id="@+id/fragment_view_tally"
        android:name="com.example.android.scoresheet.app.TallyViewFragment"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:context=".Tallies.TallyViewFragment"
        tools:layout="@layout/fragment_view_tally" />

</LinearLayout>
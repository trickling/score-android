<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:id="@+id/entrant_view_detail_container"
    tools:ignore="MergeRootFrame" >

    <android.support.v7.widget.Toolbar
        android:id="@+id/my_toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="?attr/colorPrimaryDark"
        android:titleTextColor="@color/white"
        android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
        android:popupTheme="@style/ThemeOverlay.AppCompat.Dark"/>

    <!--android:elevation="4dp"-->

    <LinearLayout
        android:orientation="vertical"
        android:id="@+id/fragment_detail_view_entrant"
        android:name="com.example.android.scoresheet.app.EntrantViewDetailFragment"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:context=".Entrants.EntrantViewDetailFragment"
        tools:layout="@layout/fragment_detail_view_entrant" />

</LinearLayout>